# Backend URL for Android / LAN / production. Leave empty for PC-only dev (Vite proxies /api).
# VITE_API_URL=https://your-viewer-api.onrender.com
# VITE_API_URL=http://192.168.1.10:5002

# Must match backend VIEWER_AUTH_TOKEN when VITE_API_URL is set.
# VITE_AUTH_TOKEN=change-me-to-a-long-secret
